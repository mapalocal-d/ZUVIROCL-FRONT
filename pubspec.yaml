name: zuvirocl_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  shared_preferences: ^2.2.2
  google_fonts: ^8.0.1
  flutter_svg: ^2.0.10
  logger: 
  url_launcher: ^6.2.5
  google_maps_flutter: ^2.5.0       # <-- AGREGADO
  geolocator: ^10.0.0               # <-- AGREGADO

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mapa_demo_actualizado.svg